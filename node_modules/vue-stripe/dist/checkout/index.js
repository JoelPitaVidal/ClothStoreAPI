import { n as CheckoutProvider, r as useCheckout, t as createElementComponent } from "../createElementComponent-D1BOrObz.js";
import { h } from "vue";

//#region src/checkout/index.ts
const CurrencySelectorElement = createElementComponent("currencySelector");
const PaymentElement = createElementComponent("payment");
const PaymentFormElement = createElementComponent("paymentForm");
/**
* @docs https://www.vue-stripe.com/getting-started/embedded-components/#elements-components
*/
const ExpressCheckoutElement = createElementComponent("expressCheckout");
const TaxIdElement = createElementComponent("taxId");
const AddressElementBase = createElementComponent("address");
const BillingAddressElement = (props) => {
	const { options,...rest } = props;
	return h(AddressElementBase, {
		...rest,
		options: {
			...options,
			mode: "billing"
		}
	});
};
const ShippingAddressElement = (props) => {
	const { options,...rest } = props;
	return h(AddressElementBase, {
		...rest,
		options: {
			...options,
			mode: "shipping"
		}
	});
};

//#endregion
export { BillingAddressElement, CheckoutProvider, CurrencySelectorElement, ExpressCheckoutElement, PaymentElement, PaymentFormElement, ShippingAddressElement, TaxIdElement, useCheckout };